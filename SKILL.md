---
name: belier-start
description: >
  BELIER 팀 워크플로우 라우터. 프로젝트/기능 작업을 시작할 때 실행하면 (1) 부족한
  스킬팩(mattpocock, codex)을 자동 설치하고, (2) 지금이 Evan 4단계(기획/스펙/개발·검증/유지)
  중 어디인지 질문 하나로 판단해 알맞은 스킬을 자동 발동하고, (3) 무엇을 왜 실행했는지 보고한다.
  트리거: "/belier-start", "프로젝트 시작", "뭐부터 하지", "어떤 스킬 써야 해",
  "belier start", "워크플로우 시작", "벨리에 스타트".
---

# /belier-start — BELIER 팀 워크플로우 라우터

팀원(대부분 비개발자)이 스킬 이름을 외우지 않고도 알맞은 스킬을 쓰게 만드는 안내 스킬.
Evan의 4단계 개발 워크플로우를 한 흐름으로 묶는다.

## 4단계 라우팅 표 (두뇌)

| Phase | 언제 | 발동 스킬 (순서대로) | 자동 활용 에이전트 |
|-------|------|----------------------|--------------------|
| 1 기획 | 새 프로젝트/기능 시작, "이거 만들까?" | `/office-hours` → `/grill-with-docs` | `planner`, `architect` |
| 2 스펙 | 어떻게 만들지 정리, 기획 승인 후 | `/brainstorming` → `/writing-plans` | `planner` (+필요시 `database-reviewer`) |
| 3 개발·검증 | 실제로 코드 짤 때, 스펙 승인 후 | `/subagent-driven-development` → `/codex review` → `/browse` | 끝나면 `code-reviewer` + `security-reviewer` |
| 4 유지 | 리팩터링, 구조 정리, 출시 후 | `/improve-codebase-architecture` | `refactor-cleaner`, `code-reviewer` |

---

## 에이전트 연동 (C+ 방식)

이 워크플로우는 **여러 에이전트가 역할을 나눠 진행**한다. 새 에이전트를 떼로 만들지 않는다 — 이미 있는 걸 단계에 맞게 부른다.

**규칙:**
1. 각 단계에서 위 표의 "자동 활용 에이전트"를 Agent 도구로 호출한다. 호출 시 1줄 보고: "이 단계는 `planner` 에이전트가 돕습니다."
2. **에이전트가 없으면 막지 않는다.** `~/.claude/agents/`나 `.claude/agents/`에 해당 에이전트가 없으면 건너뛰고 "이 단계는 ○○ 에이전트가 있으면 더 좋아요"만 안내한다.
3. **특수 역할 제안.** 그 프로젝트만의 특수 작업(예: "BELIER 발주 데이터 검증")이 반복되면, **딱 1개**를 제안한다: "이 프로젝트엔 ○○ 전담 에이전트가 있으면 좋겠는데, 만들까요?" 승인 시에만 `.claude/agents/<이름>.md`를 만든다. 남발 금지.
4. 에이전트 호출도 **단계 승인 게이트 안에서**만. 자동으로 다음 단계로 넘어가지 않는다.

---

## STEP 0 — Preflight (반드시 먼저, 매 실행)

어떤 단계로도 들어가기 전에 환경을 점검한다. 사람이 읽을 수 있는 한국어로 보고할 것.

```bash
bash ~/.claude/skills/belier-start/setup.sh --check
```

- 출력에 `MISSING:` 가 하나라도 있으면 → 사용자에게 "처음이라 필요한 도구를 설치할게요 (1~2분)"라고 말하고 설치 실행:
  ```bash
  bash ~/.claude/skills/belier-start/setup.sh
  ```
- 출력에 `CODEX_AUTH: no` 가 보이면 → Phase 3에 들어가기 전에 미리 경고:
  "코드 리뷰(codex)를 쓰려면 터미널에서 `codex login`을 한 번 직접 해야 해요. 지금 다른 단계면 그냥 진행해도 됩니다."
- 설치/네트워크 실패 시 → 기술 에러를 그대로 던지지 말고, 평이한 안내 + 수동 대안을 제시한다.

setup.sh가 모든 설치를 idempotent하게 처리한다 (이미 있으면 건너뜀). 안전하게 여러 번 실행 가능.

---

## STEP 1 — 단계 판단 (질문 하나)

사용자 입력이 이미 한 단계를 명확히 가리키면 질문을 건너뛰고 STEP 2로 간다.
예: "새 기능 만들래"=Phase 1, "이제 코드 짜자"=Phase 3.

애매하면 AskUserQuestion으로 **딱 한 번** 묻는다:

> 지금 어느 단계인가요?
> - **1 기획** — 뭘 만들지/만들 가치가 있는지부터 정하기
> - **2 스펙** — 어떻게 만들지 설계·계획 잡기
> - **3 개발·검증** — 실제로 코드 짜고 검증하기
> - **4 유지** — 이미 있는 코드 구조 정리·개선

**타이브레이크 규칙:** "이거 만들까?"(아이디어 검증)=Phase 1, "어떻게 만들지"(설계)=Phase 2.
헷갈리면 **무조건 더 앞 단계로** 보낸다 (기획을 건너뛰는 게 가장 위험).

---

## STEP 2 — 자동 발동 + 보고

판단된 Phase의 **첫 스킬**을 즉시 Skill 도구로 발동한다. 발동 직전 1줄 보고:

> "지금은 **N단계(이름)** 라서 `/스킬명`을 실행합니다."

스킬 발동 시 주의:
- `/grill-with-docs`, `/improve-codebase-architecture` → mattpocock 스킬 (Skill 도구로 호출).
- `/brainstorming`, `/writing-plans`, `/subagent-driven-development` → superpowers 스킬.
- `/office-hours`, `/browse`, `/codex` → gstack 스킬.

---

## STEP 3 — 단계 체이닝 (승인 게이트)

한 단계가 끝나면 **자동으로 다음 단계로 넘어가지 않는다.** 결과를 1줄 보고한 뒤 AskUserQuestion:

> N단계가 끝났어요. 다음 **(N+1)단계** 로 `/다음스킬`을 실행할까요?
> - 네, 다음으로
> - 아니요, 여기서 멈춤

사용자가 승인할 때만 다음 Phase의 STEP 2로 진행. (HARD GATE 정신 유지 + 비개발자에게 통제권)

---

## 중요 규칙

- Preflight를 건너뛰지 않는다 (설치 안 된 스킬로 안내하면 팀원이 막힌다).
- 질문은 한 번에 하나만.
- 무엇을, 왜 실행했는지 항상 1줄로 보고한다 (팀원이 흐름을 학습하도록).
- 단계는 자동 진행 금지. 매 단계 승인.
