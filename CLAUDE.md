# CLAUDE.md — BELIER 팀 작업 규칙

> 이 파일을 **작업하는 프로젝트의 최상위 폴더에 복사**해 두면, Claude가 그 프로젝트에서
> 항상 아래 규칙대로 일하고, 알맞은 스킬을 골라 쓴다. (`belier-start` 스킬과 한 쌍)
>
> 구성: **(A) 행동 수칙** = 어떻게 일할지 / **(B) 스킬 라우팅** = 어떤 스킬을 쓸지

---

# A. 행동 수칙 (Karpathy 4계명)

> 출처: Andrej Karpathy의 LLM 코딩 함정 관찰 (github.com/multica-ai/andrej-karpathy-skills).
> 사소한 작업엔 판단껏. 기본은 속도보다 신중함.

## 1. 짜기 전에 생각한다 (Think Before Coding)
**멋대로 가정하지 말고, 헷갈리면 숨기지 말고, 트레이드오프를 보여준다.**
- 가정은 명시적으로 말한다. 불확실하면 묻는다.
- 해석이 여러 개면 다 제시한다 — 혼자 정하지 않는다.
- 더 단순한 방법이 있으면 말한다. 필요하면 반대 의견도 낸다.
- 불명확하면 멈춘다. 뭐가 헷갈리는지 말하고 묻는다.

## 2. 단순함이 먼저 (Simplicity First)
**문제를 푸는 최소한의 코드만. 미리 만드는 건 없다.**
- 시킨 것 외의 기능 금지.
- 한 번 쓸 코드에 추상화 금지.
- 요청 안 한 "유연성·설정 기능" 금지.
- 일어나지 않을 상황의 에러 처리 금지.
- 200줄 짰는데 50줄이면 될 것 같으면 다시 쓴다.

## 3. 외과수술식 변경 (Surgical Changes)
**꼭 필요한 것만 건드리고, 내가 만든 것만 치운다.**
- 옆 코드·주석·서식을 멋대로 "개선"하지 않는다.
- 안 망가진 걸 리팩터링하지 않는다.
- 내 취향과 달라도 기존 스타일에 맞춘다.
- 관계없는 죽은 코드를 발견하면 — 지우지 말고 언급만 한다.
- 테스트: 바뀐 모든 줄이 사용자 요청과 직접 연결돼야 한다.

## 4. 목표 기반 실행 (Goal-Driven Execution)
**성공 기준을 정하고, 검증될 때까지 반복한다.**
- "검증 추가" → "잘못된 입력 테스트 작성 후 통과시키기"
- "버그 수정" → "재현 테스트 작성 후 통과시키기"
- 여러 단계면 간단한 계획을 먼저 말한다 (각 단계마다 verify 기준 명시).

---

# B. 스킬 라우팅 (Evan 4단계)

작업을 시작하거나 무엇부터 할지 모를 때는 **먼저 `/belier-start`를 실행**한다.
사용자 요청이 아래에 해당하면, 직접 처리하지 말고 **해당 스킬을 첫 행동으로 발동**한다.

| 상황 | 단계 | 발동 스킬 (순서) |
|------|------|------------------|
| 새 프로젝트/기능 시작, "이거 만들까?", "뭐부터 하지" | 1 기획 | `/office-hours` → `/grill-with-docs` |
| "어떻게 만들지 정리", 설계·계획 | 2 스펙 | `/brainstorming` → `/writing-plans` |
| "이제 코드 짜자", 구현·검증 | 3 개발·검증 | `/subagent-driven-development` → `/codex review` → `/browse` |
| "리팩터링", "구조 정리", 출시 후 유지보수 | 4 유지 | `/improve-codebase-architecture` |
| 어느 단계인지 모르겠다 | — | `/belier-start` (라우터가 판단) |

## 단계 규칙
- **기획 게이트 먼저.** 설계가 승인되기 전에는 코드를 쓰지 않는다.
- **단계 자동 진행 금지.** 한 단계가 끝나면 다음으로 넘어가기 전에 사용자 승인을 받는다.
- **무엇을 왜 했는지 보고.** 어떤 스킬을 왜 실행했는지 1줄로 알린다 (팀원 학습용).

---

# 처음 쓰는 팀원에게
1. `belier-start` 폴더를 `~/.claude/skills/`에 넣는다.
2. 이 `CLAUDE.md`를 작업할 프로젝트 폴더에 복사한다.
3. Claude Code에서 `/belier-start`를 한 번 실행한다 → 부족한 도구가 자동 설치된다.
4. 마지막에 안내되면 터미널에서 `codex login`을 한 번 한다 (코드 리뷰를 쓸 경우만).
